import * as React from 'react';
import { useTheme, CanvasProvider } from '@workday/canvas-kit-react-common';
export var StaticStates = function (_a) {
    var children = _a.children;
    var theme = useTheme();
    theme._staticStates = true;
    return React.createElement(CanvasProvider, { theme: theme }, children);
};
