export var makeMq = function (breakpoints) {
    var mq = {};
    Object.keys(breakpoints).forEach(function (key) {
        mq[key] = "@media (min-width: " + breakpoints[key] + "px)";
    });
    return mq;
};
