import { ButtonIconPosition } from '../types';
import { ButtonProps } from '../Button';
export interface ButtonLabelIconProps extends Pick<ButtonProps, 'size' | 'icon'> {
    dropdown?: boolean;
    iconPosition?: ButtonIconPosition;
}
export declare const ButtonLabelIcon: ({ icon, size, dropdown, iconPosition, ...elemProps }: ButtonLabelIconProps) => JSX.Element | null;
//# sourceMappingURL=ButtonLabelIcon.d.ts.map
